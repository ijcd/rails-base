.row
  .columns
    h2= title

.row
  .medium-6.columns
    = simple_form_for resource,
        as: resource_name,
        url: registration_path(resource_name),
        html: { method: :put } do |f|

      .form-inputs
        = f.input :full_name, required: true, autofocus: true
        = f.input :email, required: true
        = f.input :password,
            autocomplete: "off",
            hint: t('views.devise.registrations.edit.new_password.hint'),
            required: false
        = f.input :password_confirmation,
            required: false
        = f.input :current_password,
            hint: t('views.devise.registrations.edit.confirm_password.hint'),
            required: true

      .form-actions
        = f.button :submit, t('views.application.form.update')

  .medium-6.columns.end
    h6
      b= t('views.devise.registrations.edit.cancel.title')
    p
      = t('views.devise.registrations.edit.cancel.reason')
      '
      = link_to t('views.devise.registrations.edit.cancel.title'),
          registration_path(resource_name),
          data: { confirm: t('views.application.form.sure') },
          method: :delete
